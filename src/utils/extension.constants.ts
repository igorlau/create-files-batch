import type { TExtensionConfig } from "../extension.types";

export const CUSTOM_EXTENSION_CONFIG: TExtensionConfig = {
  label: "Custom",
  description: "Create multiple files based on input",
  files: [],
};
