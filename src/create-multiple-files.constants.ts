import type { TSettingsTemplate } from "./create-multiple-files.types";

export const CUSTOM_SETTINGS_TEMPLATE: TSettingsTemplate = {
  label: "Custom",
  description: "Create multiple files based on input",
  suffixes: [],
};
